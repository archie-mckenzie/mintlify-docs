---
title: "Code"
description: "Inline-Code und Codeblöcke anzeigen"
icon: "code"
---

<div id="adding-code-samples">
  ## Hinzufügen von Codebeispielen
</div>

Sie können Inline-Code-Snippets oder Codeblöcke hinzufügen. Codeblöcke unterstützen Metaoptionen für Syntaxhervorhebung, Titel, Zeilenhervorhebung, Icons und mehr.

<div id="inline-code">
  ### Inline-Code
</div>

Um ein `Wort` oder eine `Wortgruppe` als Code zu kennzeichnen, setzen Sie es in Backticks (\`) ein.

```mdx
Um ein `Wort` oder eine `Wortgruppe` als Code zu kennzeichnen, setzen Sie es in Backticks (`) ein.
```

<div id="code-blocks">
  ### Codeblöcke
</div>

Verwenden Sie [fenced code blocks](https://www.markdownguide.org/extended-syntax/#fenced-code-blocks), indem Sie Code in drei Backticks einschließen. Codeblöcke können kopiert werden, und wenn Sie den Assistenten aktiviert haben, können Nutzende die KI bitten, den Code zu erklären.

Geben Sie die Programmiersprache für die Syntaxhervorhebung an, um Metaoptionen zu aktivieren. Fügen Sie beliebige Metaoptionen, wie einen Titel oder ein Icon, nach der Sprache hinzu.

```java HelloWorld.java lines icon="java"
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

````mdx
```java HelloWorld.java lines icon="java"
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```
````

<div id="code-block-options">
  ## Codeblock-Optionen
</div>

Sie können Metaoptionen zu Ihren Codeblöcken hinzufügen, um deren Darstellung anzupassen.

<Note>
  Sie müssen eine Programmiersprache für einen Codeblock angeben, bevor Sie andere Metaoptionen hinzufügen.
</Note>

<div id="option-syntax">
  ### Optionssyntax
</div>

- **String- und boolesche Optionen**: Mit `""`, `''` oder ohne Anführungszeichen schreiben.
- **Ausdrucksoptionen**: Mit `{}`, `""` oder `''` schreiben.

<div id="syntax-highlighting">
  ### Syntaxhervorhebung
</div>

Aktivieren Sie die Syntaxhervorhebung, indem Sie die Programmiersprache nach den öffnenden Backticks eines Codeblocks angeben.

Wir verwenden [Shiki](https://shiki.style/) für die Syntaxhervorhebung und unterstützen alle verfügbaren Sprachen. Die vollständige Liste der [Sprachen](https://shiki.style/languages) finden Sie in der Shiki-Dokumentation.

```java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

````mdx
```java
class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```
````

<div id="title">
  ### Titel
</div>

Fügen Sie einen Titel hinzu, um Ihr Codebeispiel zu kennzeichnen. Verwenden Sie `title="Your title"` oder eine Zeichenkette in einer einzelnen Zeile.

```javascript Code Block Example
const hello = "world";
```

````mdx
```javascript Code Block Example
const hello = "world";
```
````

<div id="icon">
  ### Icon
</div>

Fügen Sie Ihrem Codeblock mit der Eigenschaft `icon` ein Icon hinzu. Siehe [Icons](/de/components/icons) für alle verfügbaren Optionen.

```javascript icon="square-js"
const hello = "world";
```

````mdx
```javascript icon="square-js"
const hello = "world";
```
````

<div id="line-highlighting">
  ### Zeilen hervorheben
</div>

Heben Sie bestimmte Zeilen in Ihren Codeblöcken hervor, indem Sie `highlight` mit den gewünschten Zeilennummern oder -bereichen verwenden.

```javascript Line Highlighting Example highlight= {1-2,5}
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

````mdx
```javascript Line Highlighting Example highlight={1-2,5}
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```
````

<div id="line-focusing">
  ### Zeilen fokussieren
</div>

Fokussieren Sie bestimmte Zeilen in Ihren Codeblöcken, indem Sie `focus` mit Zeilennummern oder -bereichen verwenden.

```javascript Line Focus Example focus= {2,4-5}
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

````mdx
```javascript Line Focus Example focus={2,4-5}
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```
````

<div id="show-line-numbers">
  ### Zeilennummern anzeigen
</div>

Blenden Sie Zeilennummern auf der linken Seite Ihres Codeblocks mit `lines` ein.

```javascript Show Line Numbers Example lines
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

````mdx
```javascript Show Line Numbers Example lines
const greeting = "Hello, World!";
function sayHello() {
  console.log(greeting);
}
sayHello();
```
````

<div id="expandable">
  ### Erweiterbar
</div>

Ermöglichen Sie es Nutzern, lange Codeblöcke mit `expandable` ein- und auszuklappen.

```python Expandable Example expandable
from datetime import datetime, timedelta
from typing import Dict, List, Optional
from dataclasses import dataclass

@dataclass
class Book:
    title: str
    author: str
    isbn: str
    checked_out: bool = False
    due_date: Optional[datetime] = None

class Library:
    def __init__(self):
        self.books: Dict[str, Book] = {}
        self.checkouts: Dict[str, List[str]] = {}  # patron -> list of ISBNs

    def add_book(self, book: Book) -> None:
        if book.isbn in self.books:
            raise ValueError(f"Book with ISBN {book.isbn} already exists")
        self.books[book.isbn] = book

    def checkout_book(self, isbn: str, patron: str, days: int = 14) -> None:
        if patron not in self.checkouts:
            self.checkouts[patron] = []

        book = self.books.get(isbn)
        if not book:
            raise ValueError("Book not found")

        if book.checked_out:
            raise ValueError("Book is already checked out")

        if len(self.checkouts[patron]) >= 3:
            raise ValueError("Patron has reached checkout limit")

        book.checked_out = True
        book.due_date = datetime.now() + timedelta(days=days)
        self.checkouts[patron].append(isbn)

    def return_book(self, isbn: str) -> float:
        book = self.books.get(isbn)
        if not book or not book.checked_out:
            raise ValueError("Book not found or not checked out")

        late_fee = 0.0
        if datetime.now() > book.due_date:
            days_late = (datetime.now() - book.due_date).days
            late_fee = days_late * 0.50

        book.checked_out = False
        book.due_date = None

        # Remove from patron's checkouts
        for patron, books in self.checkouts.items():
            if isbn in books:
                books.remove(isbn)
                break

        return late_fee

    def search(self, query: str) -> List[Book]:
        query = query.lower()
        return [
            book for book in self.books.values()
            if query in book.title.lower() or query in book.author.lower()
        ]

def main():
    library = Library()

    # Add some books
    books = [
        Book("The Hobbit", "J.R.R. Tolkien", "978-0-261-10295-4"),
        Book("1984", "George Orwell", "978-0-452-28423-4"),
    ]

    for book in books:
        library.add_book(book)

    # Checkout and return example
    library.checkout_book("978-0-261-10295-4", "patron123")
    late_fee = library.return_book("978-0-261-10295-4")
    print(f"Late fee: ${late_fee:.2f}")

if __name__ == "__main__":
    main()
```

````mdx
```python Expandable Example expandable
from datetime import datetime, timedelta
from typing import Dict, List, Optional
from dataclasses import dataclass

# ...

if __name__ == "__main__":
    main()
```
````

<div id="wrap">
  ### Umbruch
</div>

Aktivieren Sie den Zeilenumbruch für lange Zeilen mit `wrap`. Dies verhindert horizontales Scrollen und verbessert die Lesbarkeit langer Zeilen.

```javascript Wrap Example wrap
const greeting = "Hello, World! I am a long line of text that will wrap to the next line.";
function sayHello() {
  console.log(greeting);
}
sayHello();
```

````mdx
```javascript Wrap Example wrap
const greeting = "Hello, World! I am a long line of text that will wrap to the next line.";
function sayHello() {
  console.log(greeting);
}
sayHello();
```
````

<div id="diff">
  ### Diff
</div>

Zeigen Sie visuelle Diffs für hinzugefügte oder entfernte Zeilen in Ihren Codeblöcken. Hinzugefügte Zeilen werden grün, entfernte rot hervorgehoben.

Um Diffs zu erstellen, fügen Sie diese speziellen Kommentare am Ende der Zeilen in Ihrem Codeblock hinzu:

- `// [!code ++]`: Markiert eine Zeile als hinzugefügt (grüne Hervorhebung).
- `// [!code --]`: Markiert eine Zeile als entfernt (rote Hervorhebung).

Für mehrere aufeinanderfolgende Zeilen geben Sie die Anzahl der Zeilen nach einem Doppelpunkt an:

- `// [!code ++:3]`: Markiert die aktuelle Zeile plus die nächsten zwei Zeilen als hinzugefügt.
- `// [!code --:5]`: Markiert die aktuelle Zeile plus die nächsten vier Zeilen als entfernt.

Die Kommentarsyntax muss Ihrer Programmiersprache entsprechen (zum Beispiel `//` für JavaScript oder `#` für Python).

```js Diff Example icon="code" lines
const greeting = "Hello, World!"; // [!code ++]
function sayHello() {
  console.log("Hello, World!"); // [!code --]
  console.log(greeting); // [!code ++]
}
sayHello();
```

````text
```js Diff Example icon="code" lines
const greeting = "Hello, World!"; // [!code ++]
function sayHello() {
  console.log("Hello, World!"); // [!code --]
  console.log(greeting); // [!code ++]
}
sayHello();
```
````