---
title: "Antwortfelder"
description: "API-Antwortwerte anzeigen"
---

Die `<ResponseField>`-Komponente dient dazu, die Rückgabewerte einer API zu definieren. Viele Dokumentationen verwenden `<ResponseField>` auch auf Seiten, wenn die Typen bestimmter Elemente aufgelistet werden müssen.

<ResponseField name="response" type="string" required>
  Ein Beispiel für ein Antwortfeld
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  Ein Beispiel für ein Antwortfeld
</ResponseField>
```

<div id="props">
  ## Props
</div>

<ResponseField name="name" type="string" required>
  Der Name des Antwortwerts.
</ResponseField>

<ResponseField name="type" type="string" required>
  Erwarteter Typ des Antwortwerts – dies kann eine beliebige Zeichenfolge sein.
</ResponseField>

<ResponseField name="default" type="string">
  Der Standardwert.
</ResponseField>

<ResponseField name="required" type="boolean">
  „required“ neben dem Feldnamen anzeigen.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Ob ein Feld als veraltet gilt.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Labels, die vor dem Feldnamen angezeigt werden
</ResponseField>

<ResponseField name="post" type="string[]">
  Labels, die nach dem Feldnamen angezeigt werden
</ResponseField>

<RequestExample>

```mdx Response Field Example
<ResponseField name="response" type="string" required>
  Ein Beispiel für ein Antwortfeld
</ResponseField>
```

</RequestExample>
