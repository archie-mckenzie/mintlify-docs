---
title: "アイコン"
description: "人気のアイコンライブラリのアイコンを使う"
icon: "flag"
---

import IconsRequired from "/snippets/ja/icons-required.mdx";

Font Awesome、Lucide、SVG、外部URL、またはプロジェクト内のファイルのアイコンを使用して、ドキュメントを充実させましょう。

<Icon icon="flag" size={32} />

```mdx Icon example
<Icon icon="flag" size={32} />
```


<div id="inline-icons">
  ## インラインアイコン
</div>

段落内で使用する場合、アイコンはインラインで表示されます。

<Icon icon="flag" iconType="solid" /> 望むドキュメントを、手間なく。

```markdown Inline icon example
<Icon icon="flag" iconType="solid" /> 望むドキュメントを、手間なく。
```

<div id="properties">
  ## プロパティ
</div>

<IconsRequired />

<ResponseField name="color" type="string">
  アイコンの色を16進数コード（例：`#FF5733`）で指定します。
</ResponseField>

<ResponseField name="size" type="number">
  アイコンのサイズ（ピクセル単位）。
</ResponseField>